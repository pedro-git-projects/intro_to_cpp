#include<iostream>
//declaring namespace
using namespace std;

int main()
{
    std::cout << "Printing using scope resolution operator with namespace and object." << std::endl;
    cout << "Printing without a scope resolution operator, by means of namespace declaration." << endl;
    return 0;
}